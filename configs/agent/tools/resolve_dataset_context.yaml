name: resolve_dataset_context
description: Inspect an AnnData input and summarize its dimensions and labels.
domain: dataset
input_schema:
  dataset_path:
    type: string
  dataset_id:
    type: string
outputs:
  - context_summary.json
metadata:
  handler: resolve_dataset_context

